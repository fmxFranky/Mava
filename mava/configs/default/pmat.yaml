defaults:
  - logger: logger
  - arch: anakin
  - system: pmat/pmat
  - network: pmat_transformer
  - env: rware # [cleaner, connector, vector-connector, gigastep, lbf, mabrax, matrax, rware, smax, mpe]
  - _self_

hydra:
  searchpath:
    - file://mava/configs