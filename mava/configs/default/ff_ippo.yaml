defaults:
  - logger: logger
  - arch: anakin
  - system: ppo/ff_ippo
  - network: cnn  # [mlp, cnn]
  - env: connector  # [cleaner, connector, gigastep, lbf, mabrax, matrax, rware, smax]
  - _self_

hydra:
  searchpath:
    - file://mava/configs
